1. Make sure you have node installed. `node -v`
1. Initialise a node project `npm init -y`
1. Install jest `npm install --save-dev jest`
1. Configure jest ` goto you package.json, add a script block`
